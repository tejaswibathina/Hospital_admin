import axios from "axios";

const API_BASE_URL = "https://hospital-admin-1-t3j6.onrender.com";

const api = axios.create({
  baseURL: API_BASE_URL,
});

export default api;
import axios from "axios";

const API_BASE_URL = "https://hospital-admin-1-t3j6.onrender.com";

const api = axios.create({
  baseURL: API_BASE_URL,
});

export default api;
